# 작업 로그 2026-07-28

## 결정
- Android 클라이언트: **Kotlin + Jetpack 확정**.
- Flutter: **iOS 진행 시점에 재논의** (현재 도입 안 함).
- 온디바이스 1B 다운로드:
  - 상시 서버 인스턴스 **불필요**.
  - 파일 호스팅만 있으면 됨 (공개 URL / Cloudflare R2).
  - MVP 초기는 1B 생략·템플릿만으로도 가능. PoC는 HF 직링크, 배포 안정화 시 R2.

## 문서
- `온디바이스_무료티어_계획.md` 6.0/6.1 갱신
- `implementation_plan_phase10.md` Phase 4·7 갱신

---

## 구현 (오후)
- Local Scan Core 구현분 정리: matcher/Dex/API/웹 UI 스캐폴드
- PokéAPI 풀빌드 연결: `build.py --source pokeapi --fetch --limit 1025` → species 1025, aliases 3093, fixture match@3=1.0
- Rotom 캐릭터: LUMI 프롬프트 구조 참고해 `data/rotom_character.md` + `app/agents/pokedex_agent/prompts/rotom.py` 반영 (팬세이프, NSFW 미포함)
- 캐시 경로 `data/raw/`는 gitignore

## 19:20 Codex 작업
- 요청: prod까지의 단계별 계획 저장, 로토무 모티프 문서 반영, 실기기 연결 전 UI 구현.
- 문서:
  - `docs/specs/prod_단계별_작업계획.md` 생성.
  - `docs/specs/implementation_plan_phase10.md`에 prod 계획 문서와 실기기 전 UI 게이트 연결.
  - `docs/uiux/uiux 설계서.md`를 Rotom Dex OS 기준으로 최신화.
- 구현 예정:
  - FastAPI 루트(`/`)에서 Rotom Dex OS 웹 UI 제공.
  - `/v1/scan/text` fixture 스캔 API 추가.
  - 스캔, 후보, 상세, 템플릿 대화, 로컬 컬렉션 UI 완성.
- 구현 완료:
  - `app/ui/` 정적 UI와 추상 Rotom core asset 추가.
  - `app/main.py`에서 `/` 및 `/ui/*` 서빙.
  - `app/api/routes/scan.py`에 `/v1/scan/text` 추가.
  - `tests/test_ui_predevice.py` 추가.
- 검증:
  - `python -m unittest tests/test_local_scan_core.py`
  - `uv run python -m unittest tests/test_ui_predevice.py`
  - `python -m compileall app scripts tests`
  - 브라우저 렌더링 확인: 기본 fixture 자동 스캔, 피카츄 후보/상세 표시, desktop/mobile overflow 없음.

## 19:45 Codex 보완
- 사용자 피드백:
  - YouTube 로토무 음성 기반 STS 가능성 검토.
  - 검정 UI보다 로토무의 밝은 색감과 형태를 더 반영.
  - LLM 응답이 템플릿처럼 보이는 문제 확인.
- 결정:
  - YouTube/공식 캐릭터 음성 추출 및 voice cloning은 비허용. 직접 녹음/허가 음성/공개 라이선스 음성만 STS/TTS에 사용.
  - UI는 밝은 오렌지/코랄 바디, 청록 전기, 둥근 디바이스 형태를 강화.
  - 로컬 Ollama가 있으면 LLM 옵션으로 사용하고, 실패 시 템플릿 fallback.
- 구현:
  - `app/agents/pokedex_agent/tools/tool_local_llm.py` 추가.
  - `/v1/chat` 응답에 `llm_runtime`, `llm_model` 추가.
  - UI header/chat badge에 LLM runtime 표시.
  - UI에 STS 보이스 상태 패널 추가.

## 19:55 Codex 보완
- 사용자 피드백:
  - prod 목표라면 1B 모델을 사용해야 한다는 지적.
  - `http://127.0.0.1:8000/` 페이지가 보이지 않는 문제.
- 조치:
  - 종료된 dev server를 재시작하고 루트/스캔 API 응답 확인.
  - 인앱 브라우저를 `http://127.0.0.1:8000/`로 다시 이동해 렌더링 확인.
  - `llama3.2:1b` Ollama 모델 다운로드 진행.
  - 기본 로컬 LLM 모델을 `llama3.2:1b`로 변경.

## 15:05 Codex 보완
- 조치:
  - `llama3.2:1b` Ollama 모델 다운로드 완료 및 `ollama list` 확인.
  - dev server를 재시작해 `http://127.0.0.1:8000/`를 다시 제공.
  - 루트 `HEAD /`를 200으로 보완해 프리뷰/헬스체크 실패처럼 보이는 문제를 줄임.
  - `/v1/chat/stream` SSE 추가. UI는 스트리밍을 우선 사용하고 실패 시 `/v1/chat` 완성형 응답으로 fallback.
  - 브라우저 UI에서 `ollama · llama3.2:1b` 배지와 채팅 말풍선 응답 확인.
- 검증:
  - `uv run python -m unittest tests/test_local_scan_core.py tests/test_ui_predevice.py`
  - `python -m compileall app scripts tests`
  - `python scripts/build_local_dex/validate_dex.py data/dex.sqlite`
  - `git diff --check`

## 15:20 Codex 보완
- 사용자 피드백:
  - 모바일 최적화 여부 확인.
  - STS가 UI만 있는지 확인.
  - 로토무 시스템 프롬프트에 배경/캐릭터 시놉시스 보강 요청.
  - 공식 음성 추출 없는 AI 음성 정책 반영 요청.
- 조치:
  - Rotom Dex OS system prompt를 전기 안내자 배경 시놉시스, 성격, 말투, FACTS 가드레일을 포함하도록 확장.
  - `/v1/voice/status`, `/v1/voice/preview` 추가.
  - Voice Core UI에 browser speech synthesis 기반 프리뷰, style/pitch/speed/stop 컨트롤 추가.
  - 모바일 360px 대응 CSS 보강.
  - 정책 문서에는 독자 AI 합성 음성 허용, 공식 캐릭터/성우 음색 모방 및 혼동 목적 금지를 반영.
