# 작업 로그 2026-07-28 (UI / RAG / Collector)

## 1. UI (`docs/rotomu`)
- 참고 이미지를 `app/ui/assets/rotomu/`에 복사 (kanto/sinnoh pokedex, rotom phone, expressions).
- 웹 UI를 포켓덱스 클램셸 + 로토폰 프레임으로 재구성.
- 표정 상태(`idle/think/lock/surprise/error/happy`)와 추천 질문 칩 추가.

## 2. ThinkFlow 패턴 이식 (경량)
- 세션/`flow_state` 캐리오버: `app/agents/pokedex_agent/session/`
- Hybrid retrieve: lexical + hash embedding RRF
- Multi-hop: evolution_rules + type_chart
- 멀티턴 suggested_actions, session_id 응답

## 3. RSLF 수집 개선
- `dex_passages` + hash embeddings 빌드
- `scripts/build_local_dex/collector.py` dry-run → staging → atomic swap
- passage_count=3532 (pokeapi 1025 기준)
